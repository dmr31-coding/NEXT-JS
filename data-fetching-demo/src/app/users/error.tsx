// yaha error huda dekhaune code lekhinxa

"use client";

import { error } from "console"
import { useEffect } from "react"

export default function ErrorPage() {
    // useEffect(() => {
    //     console.log(`${error}`);
    // }, [error])
    return (
        <h1>This is error page</h1>
    )
}